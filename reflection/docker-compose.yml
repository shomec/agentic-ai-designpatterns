version: '3.8'

services:
  app:
    build: .
    volumes:
      - .:/reflection
      - /var/run/docker.sock:/var/run/docker.sock
    command: [ "Write a java code to calculate fibonacci", "--use-ollama", "--ollama-url", "http://ollama:11434" ]
    depends_on:
      init-ollama:
        condition: service_completed_successfully

  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

  init-ollama:
    image: ollama/ollama
    volumes:
      - ollama_data:/root/.ollama
    entrypoint: /bin/sh
    command: "-c 'sleep 5; OLLAMA_HOST=ollama:11434 ollama pull gemma3:1b'"
    depends_on:
      - ollama

volumes:
  ollama_data:
